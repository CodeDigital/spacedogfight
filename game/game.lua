game = {}

function game.load(plyData, connection)

    -- player = plyData;
    -- players = connection.players;

end

function game.update(dt)
    if love.keyboard.isDown(key (KeyConstant))
    elseif love.keyboard.isDown(key (KeyConstant))
end

return game